
## ⚠️ Важные нюансы (записал в доку и память)

- Скрипты в `.venv/bin/` (alembic/uvicorn/celery) имеют битый shebang от старого пути → **всегда** запускай через `../.venv/bin/python -m <модуль>`.
- `pnpm` не в PATH: ставить `corepack pnpm install`, а дев запускать `./node_modules/.bin/next dev`.
- ⚠️ Твой **API-ключ засветился в чате** — после тестов перевыпусти.
- Бэк отдаёт `ig_password`/cookies в ответе аккаунта (наследие схемы) — до прода зашифровать.
- Старые pytest-тесты, вероятно, отв
-

1. Поднять 5 процессов: Postgres, Redis, FastAPI, **Celery worker (+beat)**, фронт.
2. Завести 1 тест-аккаунт через UI + рабочий прокси + свежие cookies.
3. 🔴 **Живой прогон IG**: короткий warmup на тест-аккаунте, смотреть лог воркера.
4. Доступ кенту: `ngrok http 8000` → URL в Vercel `NEXT_PUBLIC_API_URL` → redeploy → ссылка.

**P1 — ПОСЛЕ теста (во время будут мешать):** rate-limit на `/auth`, отключить `/docs` в проде, сузить CORS, **перевыпустить LLM-ключ**.

**P2 — потом:** seed-данные, Sims-таскбар, SSE для Activity, `started_at/completed_at` у задач.

## Что протестить завтра ДО созвона (по порядку)

**1. Поднять стек** (команды в доке; помни `python -m` и `./node_modules/.bin/next dev`). Проверка: `curl localhost:8000/health` → ok, воркер пишет `ready`.

**2. Смок UI (1 юзер, ~10 мин)** — войти `antivirys18@gmail.com / secret1234` (админ) и прокликать:

- Login/logout, сайдбар (есть Admin, нет Proxies).
- Accounts → Connect (прокси прямо в форме) → Edit/Proxy → Validate.
- Orchestrator: промпт → редактируемый план → таргетинг.
- Admin: видны CPU/RAM, выдай **себе** агентов (2-3) и тариф.
- F12 → Console/Network: красных ошибок быть не должно.

**3. 🔴 Живой IG-прогон (делать первым делом, тут риск!)** — на **одноразовом** аккаунте: «warm up @test for 2 minutes» → Approve → смотреть лог воркера → Activity покажет running → Tasks → completed. Если упало — причина в Tasks → `error_log`. Частые грабли: протухшие cookies, нерабочий прокси (`low_trust_score`), checkpoint. _Если не заведётся — на созвоне показываешь весь флоу до fan-out, а браузерный слой «дотачиваем»._

**4. Доступ кенту** — ngrok + Vercel, потом **открой Vercel-ссылку с телефона по мобильному интернету** (не своим Wi-Fi) и проверь логин. Заработало → зови кента, выдай ему агентов в /admin.

**Перед самим созвоном (15 мин):** все процессы живы, ngrok+Vercel актуальны, 1 IG-прогон уже виден в Activity, лог воркера открыт на видном месте, блокнот для багов.

Хочешь — пока есть время, могу сейчас сделать P1-хардening (rate-limit/CORS/выключение docs так, чтобы не мешало завтра) или seed-данные для красивого демо. Скажи — сделаю.